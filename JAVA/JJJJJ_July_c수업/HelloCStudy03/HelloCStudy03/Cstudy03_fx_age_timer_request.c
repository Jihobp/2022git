#include<stdio.h>
int main()
{
	//첫번째 문제
	int num;
	if (num )



	//두번째 문제
	int age;
	scanf_s("%d", &age);
	if (age > 20)
		printf("미성년자\n");

	if (age < 50 || age>= 20)
		printf("사회의 주 구성원\n");

	if (age <= 70 || age>=51)
		printf("관리자\n");
	
	if (age >= 71)
		printf("지혜의 도서관");

	if (age < 0, age > 200)
		printf("타노스");


	//3번 동준이의 알람시계 45분전 알람
	int H, M;
	scanf_s("%d,%d", M = 1, H = 60M)



	return 0;
}